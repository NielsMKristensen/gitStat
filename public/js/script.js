// let cb = documents.getElementsByClassName('repoCheckbox');

// console.log(cb.length);

document.addEventListener(
  "DOMContentLoaded",
  () => {
    console.log("gitStat JS imported successfully!");
  },
  false
);

